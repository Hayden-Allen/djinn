- asset "libraries" that prevent loading same thing twice? cpp or js
- Asset.Shader.bindCamera(CameraID)? automatically update before draw
- how organize state graph? separate files? do states own things? subclass?

- mesh textures
- mesh_instance_batch should ACTUALLY BATCH
- Asset.Mesh.getActionNames
	- AnimatedMesh.setActionLike("run") picks action with closest name match
- skybox
- tune jump/walk constants
- tune camera position/speed
